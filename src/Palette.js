import React, { Component } from 'react';

export default class Palette extends Component {
  render() {
    return (
      <div className="Palette">
        {/* Navbar  */}
        <div className="Palette-colors">{/* Color boxes into here */}</div>
        {/* footer */}
      </div>
    );
  }
}
